name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # cada 5 minutos
  workflow_dispatch:         # permitir ejecutarlo manualmente si querés

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Service
        run: |
          curl -s https://portalcliente.greensunriseenergy.es/client/ > /dev/null
